// TEMP STUB — ActivitySection
// Dejado intencionalmente sin lógica hasta que se reactive.
// Esto evita imports a funciones no exportadas (apiGetCustomerLog) en el build.

import type { FC } from 'react'

const ActivitySection: FC = () => {
  return null
  // Si prefieres ver algo visual:
  // return <div style={{padding: 12, opacity: 0.6}}>Actividad deshabilitada temporalmente</div>
}

export default ActivitySection
